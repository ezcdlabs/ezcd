version: 2

builds:
  - id: cli
    main: ./cmd/cli/main.go
    goos:
      - linux
      - darwin
      - windows
    goarch:
      - amd64
      - arm64
    ldflags: -s -w
    binary: ezcd-cli
  
  - id: server
    main: ./cmd/server/main.go
    goos:
      - linux
    goarch:
      - amd64
    ldflags: -s -w
    binary: ezcd-server

dockers:
  - image_templates:
    - 'ghcr.io/ezcdlabs/ezcd-server:{{ .Tag }}'
    dockerfile: Dockerfile
    goos: linux
    goarch: amd64
    ids:
      - server
    # registries:
    #   - ghcr.io

archives:
  - id: cli
    builds:
      - cli
    format: tar.gz
    name_template: "{{ .ProjectName }}_{{ .Version }}_{{ .Os }}_{{ .Arch }}"